# Evaluation results

Use `<condition>-<case>-runNN.md`; never edit a raw result. Record model, settings, prompt hash, source hashes, skill commit, and timestamp. Never upload an unpublished script to a network service for evaluation.

